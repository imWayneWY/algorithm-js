// LIFO
// JS doesn't have stack, but has array for this

const stack = [];
stack.push(1);
stack.push(2);
const item1 = stack.pop();
const item2 = stack.pop();

// example: decimal to binary, valid brackets
